# Podstawy konfiguracji i środowiska
python-dotenv

# Microsoft Graph (używamy 0.2.2 bo nowsze wersje zmieniły API - GraphClient jest w msgraph.core)
msgraph-core==0.2.2

# Uwierzytelnianie i SDK Azure
azure-identity
azure-mgmt-resource
azure-mgmt-compute
azure-mgmt-network
azure-mgmt-costmanagement
azure-mgmt-authorization
azure-mgmt-storage

# gRPC + Protobuf (zaktualizowane do najnowszych wersji kompatybilnych z protobuf 6.x)
grpcio>=1.76.0
grpcio-tools>=1.76.0
protobuf>=6.31.1
